# Provider credentials. Keep values only in .env or process env, never in git.
PTE_REMOTE_RUNPOD_ALL=
PTE_REMOTE_WANDB_API=
PTE_REMOTE_HF_WRITE=
PTE_REMOTE_HYPERBOLIC_GPU=

# Generic chat-completions HTTP endpoint, including hosted vLLM.
# configs/gsm8k_hyperbolic_api_smoke.yaml does not require these generic names.
PTE_REMOTE_BASE_URL=
PTE_REMOTE_API_KEY=
PTE_REMOTE_MODEL=
